import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  btn: {
    position: 'absolute',
    bottom: 112,
    right: 16,
    width: 90,
    height: 90,
    zIndex: 1000,
    alignItems: 'center',
    justifyContent: 'center',
  },
  robot: {
    width: 90,
    height: 90,
  },
})
