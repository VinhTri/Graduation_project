import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: "100%",
    paddingBottom: 40,
  },
  listContainer: {
    width: "100%",
    position: "relative",
    paddingTop: 24, // Space for peeping cards of the first wallet
  },
});
export default styles;
